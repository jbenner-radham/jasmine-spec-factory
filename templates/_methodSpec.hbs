describe("{{name}}", function () {
    it("is a function", function () {
        expect(module.{{name}}).toEqual(jasmine.any(Function));
    });
    {{#if returnsUndefined}}

    {{> returnsUndefined }}
    {{/if}}
});
